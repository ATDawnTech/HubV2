/**
 * Public API of the dashboard feature module.
 *
 * Only export what other features legitimately need. Internal components,
 * hooks, and services are not exported — they are private to this feature.
 */

export { DashboardPage } from "./pages/DashboardPage";
